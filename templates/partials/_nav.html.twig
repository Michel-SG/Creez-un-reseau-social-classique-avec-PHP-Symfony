
<nav class="navbar navbar-expand-sm navbar-dark bg-primary">
    <a class="navbar-brand ml-5" href="{{ path('app_displaypins') }}">PintsPage</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse " id="navbarColor01">
        <ul class="navbar-nav ml-auto mr-5 mb-sm-0">
            {% if not app.user %}
            <li class="nav-item active">
                <a class="nav-link " href="{{ path('app_signin') }}">Connection</a>
            </li>
            <li class="nav-item">
                <a class="nav-link  " href="{{ path('app_signup') }}">Inscription</a>
            </li>
            {% else %}
            <li class="nav-item">
                <a class="nav-link  " href="{{ path('app_profile') }}">Pofile</a>
            </li>
            <li class="nav-item">
                <a class="nav-link  " href="{{ path('app_displaypins') }}">Pints</a>
            </li>
            <li class="nav-item">
                <a class="nav-link  " href="#" onclick="event.preventDefault(); document.getElementById('js-csrf-logout').submit();">deconnection</a>
            </li>
            <form id="js-csrf-logout" action="{{ path('app_logout') }}" method="POST">
                <input type='hidden' name="_csrf_token" value ="{{ csrf_token('logout') }}"/>
            </form>
            {% endif %} 
        </ul>
    </div>
</nav>